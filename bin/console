#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "get_process_mem"
puts "The memory size before load 'wasval' is #{GetProcessMem.new.mb}MB"
require "wasval"
puts "The memory size after loaded 'wasval' is #{GetProcessMem.new.mb}MB"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

require "irb"
require "irb/reload"
require "debug"
IRB.start(__FILE__)
